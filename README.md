# AirFreightIntelligence
(REPO was not used throughout an instead will act as a code dump)

A machine learning system to forecast freight demand and detect anomalies at the lane level.

The dataset used in this project is not included in this repository due to privacy and size constraints. The code assumes access to lane-level shipment data with fields such as origin, destination, shipment weight, and event date.

## Note on Sensitive Code

Some portions of the original implementation have been omitted due to sensitive considerations. These components were related to internal or company specific logic and are not required to understand the overall modeling approach.

The repository still reflects the full pipeline and demonstrates how forecasting and anomaly detection were implemented and evaluated.

## Notes

- Code is provided as-is for evaluation purposes
- Paths and dataset references may need to be adjusted to run locally
